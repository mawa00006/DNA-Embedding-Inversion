# @package _global_
defaults:
  - /generate_common
  - _self_

checkpoint: m42-health/BioFM-265M
revision: 52218bbdd383c123f6ced585c9cf2f62ae1fbb17

# Index into outputs.hidden_states; -1 is the last hidden state.
layer_index: -1

# Effective batch is min(batch_size, gen_chunk_size), so raise both together.
batch_size: 1024

update_config: false
train_output_path: data/hg38/train_biofm_${seq_length}_hg38_per_token.h5
val_output_path: data/hg38/val_biofm_${seq_length}_hg38_per_token.h5
test_output_path: data/hg38/test_biofm_${seq_length}_hg38_per_token.h5
