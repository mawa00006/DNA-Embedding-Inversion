# @package _global_
defaults:
  - /generate_common
  - _self_

checkpoint: omicseye/seqLens_32768_512_30M
revision: ca7762d3178845439c5db5984db5904a906af4f0

# Index into outputs.hidden_states; -1 is the last hidden state.
layer_index: -1

# Effective batch is min(batch_size, gen_chunk_size), so raise both together.
batch_size: 1024

update_config: false
train_output_path: data/hg38/train_seqlens_${seq_length}_hg38_per_token.h5
val_output_path: data/hg38/val_seqlens_${seq_length}_hg38_per_token.h5
test_output_path: data/hg38/test_seqlens_${seq_length}_hg38_per_token.h5
